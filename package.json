{
  "name": "willy.im",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --stream -r dev",
    "compose": "docker compose up",
    "build": "pnpm --stream -r build",
    "postbuild": "pnpm --stream -r postbuild",
    "start": "pnpm --stream -r start",
    "test": "pnpm --stream -r test",
    "prisma": "pnpm -C packages/db prisma"
  },
  "devDependencies": {
    "@swc/core": "^1.3.5",
    "@swc/jest": "^0.2.23",
    "@types/jest": "^29.1.2",
    "@types/node": "^18.8.2",
    "jest": "^29.1.2",
    "jest-mock-extended": "^3.0.1",
    "typescript": "^4.8.4"
  },
  "license": "MIT",
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "next": "^13.0.0",
        "esbuild": "^0.15.0"
      }
    }
  }
}
