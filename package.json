{
  "name": "willy.im",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --stream -r dev",
    "compose": "docker compose up",
    "dev2": "pnpm compose && pnpm dev",
    "build": "pnpm --stream -r build",
    "postbuild": "pnpm --stream -r postbuild",
    "start": "pnpm --stream -r start",
    "test": "pnpm --stream -r test",
    "prisma": "pnpm -C packages/db prisma"
  },
  "devDependencies": {
    "@swc/core": "^1.2.242",
    "@swc/jest": "^0.2.22",
    "@types/jest": "^28.1.8",
    "@types/node": "^18.7.13",
    "jest": "^28.1.3",
    "jest-mock-extended": "^2.0.7",
    "typescript": "^4.7.4"
  },
  "license": "MPL-2.0"
}
