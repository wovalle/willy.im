name: Test rbac

on:
  pull_request:
    paths:
      - "packages/rbac/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci
        working-directory: packages/rbac

      - name: Build
        run: npm run build
        working-directory: packages/rbac

      - name: Test
        run: node --test ./dist/test/**/*.test.js
        working-directory: packages/rbac
